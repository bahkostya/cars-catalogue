import React from 'react';
import UnstyledDropdown from 'react-dropdown';

import styles from './Dropdown.module.scss';

const Dropdown = () => <UnstyledDropdown className={styles.container} />;

export default Dropdown;
